import CTA from "@/components/CTA";
import Hero from "@/components/about/locations/Hero";

const page = () => {
  return (
    <main>
      <Hero />
      <CTA />
    </main>
  );
};

export default page;
